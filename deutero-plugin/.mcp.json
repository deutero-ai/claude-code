{
  "mcpServers": {
    "deutero": {
      "url": "https://agents.deutero.ai/mcp",
      "transport": "sse",
      "headers": {
        "X-API-Key": "${DEUTERO_API_KEY}"
      },
      "timeout": 60000
    }
  }
}
